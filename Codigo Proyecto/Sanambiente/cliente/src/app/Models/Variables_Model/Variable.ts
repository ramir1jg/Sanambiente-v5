/* Clase que contiene y exporta la interfaz con el modelo (campos) necesario para variables */

export interface Variable {
    id_variable?: number; 
    nombre_variable?: string;
    observacion_variable?: string
}