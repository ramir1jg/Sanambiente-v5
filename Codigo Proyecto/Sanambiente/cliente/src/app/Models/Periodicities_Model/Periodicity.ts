/* Clase que contiene y exporta la interfaz con el modelo (campos) necesario para periodicidad */

export interface Periodicity {
    id_periodicidad?: number; 
    tipo_periodicidad?: string;
    observacion_periodicidad?: string
}