/* Clase que contiene y exporta la interfaz con el modelo (campos) necesario para Categoria */

export interface Category {
    id_categoria?: number;
    nombre_categoria?: string;
    observacion_categoria?: string;
}