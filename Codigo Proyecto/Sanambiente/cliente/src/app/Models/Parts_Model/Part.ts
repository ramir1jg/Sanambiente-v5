/* Clase que contiene y exporta la interfaz con el modelo (campos) necesario para parte */

export interface Part {
    id_parte?: number;
    nombre_parte?: string;
    codigo_catalogo?: string;
    observacion_parte?: string
}