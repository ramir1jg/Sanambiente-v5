/* Clase que contiene y exporta la interfaz con el modelo (campos) necesario para tipo de mantenimiento */

export interface MaintenanceType {
    id_tipo_mantenimiento?: number;
    nombre__tipo_mantenimiento?: string;
    observacion__tipo_mantenimiento?: string;
}